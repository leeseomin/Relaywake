# Cloudflare 공개 배포 필수 반영사항

## 범위와 완료 기준

이 문서는 `refine.md` 가운데 **현재 앱을 Cloudflare에 공개 배포하기 전에 반드시 해결해야 하는 항목만** 남긴 릴리스 기준이다.

- 배포 대상은 Cloudflare Workers Static Assets와 최종 커스텀 도메인이다.
- 앱은 도메인 루트(`/`)에 배포한다.
- 게임 디자인 확장, 밸런스 조정, 보스 재설계, 성장 시스템 개편, 대규모 리팩터링은 이 문서의 범위가 아니다.
- 아래 모든 항목과 최종 체크리스트를 통과해야 프로덕션 배포가 가능하다.

---

## 1. 프로덕션에서 E2E·치트 경로 완전 제거

### 현재 문제

`src/App.vue`가 URL의 `?e2e=1`만으로 E2E 모드를 활성화한다. 이 모드는 결정론적 시드, 짧은 런, 낮은 적 상한과 `window.__C2_GAME__` 테스트 브리지를 프로덕션에서도 노출할 수 있다.

### 필수 변경

1. E2E 빌드와 프로덕션 빌드를 분리한다.
2. 테스트 브리지와 테스트 전용 메서드는 E2E 어댑터로 이동한다.
3. 프로덕션 빌드에서는 컴파일 시점에 E2E 어댑터와 테스트 전용 분기가 제거되게 한다.
4. E2E 빌드 산출물은 Cloudflare에 배포하지 않는다.
5. 릴리스 파이프라인에서 실제 프로덕션 산출물을 대상으로 브리지 부재 테스트를 수행한다.

### 완료 조건

- 프로덕션 URL에 `?e2e=1`을 붙여도 게임 규칙이 바뀌지 않는다.
- `window.__C2_GAME__`는 항상 `undefined`이다.
- 런 시간은 600초, 일반 적 상한은 420으로 유지된다.
- 프로덕션에서 테스트용 고정 시드가 사용되지 않는다.
- E2E 테스트는 별도의 E2E 빌드에서 계속 통과한다.

---

## 2. Cloudflare Workers Static Assets 배포 구성 고정

### 필수 변경

1. 저장소 루트에 `wrangler.jsonc`를 추가한다.
2. Wrangler를 개발 의존성으로 버전 고정한다.
3. 프로덕션 빌드 출력은 `dist`로 고정한다.
4. SPA 탐색 요청은 `index.html`로 처리한다.
5. 현재 배포는 도메인 루트이므로 Vite 기본 `base: '/'`와 `/assets/...` 경로를 유지한다.
6. `base: './'` 또는 GitHub Pages용 서브패스 설정은 적용하지 않는다.

최소 구성은 다음과 같다.

```jsonc
{
  "$schema": "./node_modules/wrangler/config-schema.json",
  "name": "<확정된-제품명>",
  "compatibility_date": "2026-08-01",
  "assets": {
    "directory": "./dist",
    "not_found_handling": "single-page-application"
  }
}
```

서버 코드가 없는 현재 구조에는 `main` Worker 엔트리나 Assets binding을 추가하지 않는다.

### 완료 조건

- `npm run build`가 `dist`를 생성한다.
- Wrangler dry-run이 성공한다.
- `/`와 `index.html`이 정상 응답한다.
- `src/game/assets.ts`에 등록된 모든 이미지가 실제 배포 URL에서 HTTP 200을 반환한다.
- JS, CSS, 이미지의 MIME 타입이 올바르다.
- 존재하지 않는 정적 파일은 게임 HTML로 잘못 응답하지 않는다.
- 하나의 배포 파일도 Cloudflare의 개별 정적 자산 제한을 넘지 않는다.

---

## 4. IndexedDB 복구·마이그레이션과 최종 Origin 확정

### 현재 문제

현재 Dexie DB는 `c2-nightfall` v1이고, 읽기 과정에서 Zod `parse()`를 사용한다. 손상되거나 구버전인 레코드 하나가 앱 초기화를 반복적으로 막을 수 있다.

또한 IndexedDB는 Origin별 저장소이므로 `workers.dev`, 배포 미리보기 주소, `pages.dev`, 최종 커스텀 도메인의 데이터가 서로 공유되지 않는다.

### 필수 변경

1. 프로덕션 공개 전에 최종 커스텀 도메인을 확정한다.
2. 실제 사용자 진행 데이터는 최종 도메인에서만 누적하게 한다.
3. DB 이름과 스키마 버전을 릴리스 정책으로 고정한다.
4. 제품명 변경으로 DB 이름을 바꿀 경우 기존 DB를 읽는 일회성 마이그레이션을 제공한다.
5. 읽기 시 `safeParse()`를 사용하고 다음 순서로 복구한다.

```text
현재 스키마 검증
→ 구버전 데이터 마이그레이션
→ 손상 데이터 격리 또는 백업
→ 기본 프로필 생성
```

6. 저장 실패를 사용자에게 표시하고 재시도 가능한 상태로 남긴다.
7. 손상 데이터, 이전 DB 버전, 트랜잭션 실패 테스트를 CI에 추가한다.

### 완료 조건

- 손상된 프로필이나 설정이 있어도 앱이 정상적으로 시작된다.
- 복구 후 새로고침해도 같은 오류가 반복되지 않는다.
- 지원 대상 구버전 데이터가 손실 없이 이전된다.
- 같은 런 결과가 중복 저장되지 않는다.
- DB 이름 변경만으로 기존 사용자 데이터가 초기화되지 않는다.
- 미리보기 도메인의 데이터를 프로덕션 데이터로 오인하지 않는다.

---

## 5. 제품 식별자·공개 문서·자산 정책 통일

### 필수 변경

1. 최종 제품명을 하나로 확정한다.
2. 다음 위치의 이름과 버전을 동일하게 맞춘다.

- `package.json`
- `index.html` title과 description
- 게임 내 로컬라이제이션
- README와 릴리스 문서
- Wrangler 프로젝트 이름
- 스크린샷·배포 파일명
- IndexedDB 이름 또는 해당 이름을 보존하는 마이그레이션 문서

3. README의 능력 수를 실제 카탈로그와 일치시킨다.
4. 공개 소개에서는 게임 자체 설명을 먼저 사용하고, 기반 프로젝트 정보는 Credits와 `THIRD_PARTY_NOTICES.md`에 둔다.
5. 모든 외부 자산에 대해 원본 URL, 저작자, 라이선스, 수정 여부를 확인한다.
6. 출처나 사용 권한을 확정할 수 없는 자산은 공개 전에 교체한다.
7. 깨끗한 checkout에서 실패하는 `generate:enemies` 명령은 다음 중 하나로 정리한다.

- `monster-generator.html`과 필요한 생성 소스를 저장소 내부에 포함하고 경로를 수정한다.
- 생성기를 공개할 수 없다면 명령을 제거하고 PNG를 고정 산출물로 문서화한다.

### 완료 조건

- 현재 제품명과 과거 마이그레이션 설명을 제외하고 Signalfall·Relaywake·C2 이름이 혼재하지 않는다.
- README의 캐릭터·적·능력 수가 코드와 일치한다.
- 모든 배포 자산의 출처와 사용 권한을 설명할 수 있다.
- `package.json`에 노출된 모든 명령이 깨끗한 checkout에서 재현 가능하다.

---

## 최종 프로덕션 배포 체크리스트

- [ ] 프로덕션에서 `window.__C2_GAME__`가 존재하지 않는다.
- [ ] `?e2e=1`이 시드, 런 시간, 적 상한 또는 저장 결과를 바꾸지 않는다.
- [ ] E2E 빌드와 프로덕션 빌드가 분리되어 있다.
- [ ] `wrangler.jsonc`가 `dist`를 Workers Static Assets로 배포한다.
- [ ] Vite base와 게임 에셋 경로가 도메인 루트 배포에 맞는다.
- [ ] 실제 배포 URL에서 핵심 화면·게임·에셋 스모크 테스트가 통과한다.
- [ ] 손상·구버전 IndexedDB가 앱 시작을 반복적으로 막지 않는다.
- [ ] 최종 커스텀 도메인과 DB 마이그레이션 정책이 확정되어 있다.
- [ ] 제품명, 버전, 문서와 배포 식별자가 일치한다.
- [ ] 모든 외부 자산의 출처와 사용 권한이 확정되어 있다.
- [ ] 깨끗한 checkout에서 공개된 빌드·검증·생성 명령이 성공한다.

위 체크리스트가 모두 충족되기 전에는 프로덕션 공개 배포를 진행하지 않는다.
