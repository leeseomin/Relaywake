# 종합 판정

현재 상태는 **비공개 테스트·소규모 공개 알파에는 충분하지만, “최종 공개판/1.0”으로 선언하기에는 아직 이릅니다.**

좋은 점은 분명합니다. Vue와 Phaser의 경계가 비교적 명확하고, Zod 기반 데이터 검증, 시드 RNG, 오브젝트 풀, 공간 해시, Dexie 트랜잭션, 모바일 E2E 등 기본 구조는 일반적인 프로토타입보다 훨씬 잘 갖춰져 있습니다. **전체 엔진을 다시 만들 필요는 없습니다.**

다만 공개 전에 다음 세 축을 먼저 고쳐야 합니다.

1. **배포판에 테스트·치트 경로가 그대로 노출되는 문제**
2. **무기 스탯이 실제 행동과 맞지 않아 일부 강화가 무효 또는 오작동하는 문제**
3. **보스·성장·상자 구조가 장기 플레이의 재미를 약하게 만드는 문제**

제 판단은 다음과 같습니다.

| 영역          |    현재 평가 | 공개 전 판단         |
| ----------- | -------: | --------------- |
| 기본 아키텍처     |       양호 | 유지하며 분리 개선      |
| 전투 코어 정확성   |    수정 필요 | 1.0 전 필수        |
| 성장·빌드 다양성   |       부족 | 1.0 전 필수        |
| 보스전         | 미완성에 가까움 | 1.0 전 필수        |
| 성능 설계       |   기반은 있음 | 최대 부하 검증 필요     |
| 저장 시스템      | 기본 구조 양호 | 마이그레이션·복구 필요    |
| 테스트         | 범위는 좋은 편 | 실전 소크·배포 테스트 부족 |
| 공개 파이프라인    |      불완전 | 공개 전 필수 정비      |
| 이름·문서·자산 출처 |       혼재 | 공개 전 정리         |

---

# 1. 가장 먼저 수정해야 할 공개 차단 항목

## 1.1 배포판에서 `?e2e=1` 테스트 모드가 열림

가장 우선순위가 높습니다.

`src/App.vue:33`에서 URL의 `?e2e=1`만 확인하고, `src/App.vue:50-54`와 `src/game/GameController.ts:40-51`에서 전역 테스트 브리지 `window.__C2_GAME__`를 노출합니다.

이 브리지에서는 다음과 같은 조작이 가능합니다.

* 경험치 강제 지급
* 플레이어 강제 피해
* 적 강제 생성
* 최종 보스 즉시 처치
* 런 강제 종료
* 테스트용 24초 플레이 모드

또한 이 결과가 IndexedDB 프로필과 코인에 저장될 수 있습니다. 싱글플레이 게임이라 서버 보안 침해는 아니지만, 공개 빌드 무결성을 깨고 향후 랭킹·업적·텔레메트리 도입을 어렵게 만듭니다.

### 권장 수정

URL 조건만 사용하지 말고 컴파일 시점에 제거해야 합니다.

```ts
const e2eEnabled =
  import.meta.env.MODE === 'test' &&
  import.meta.env.VITE_E2E === '1' &&
  new URLSearchParams(location.search).has('e2e');
```

더 좋은 방식은 다음입니다.

* 프로덕션 빌드에는 브리지 코드 자체가 번들되지 않게 분리
* `GameController`의 테스트 메서드를 별도 E2E 어댑터로 이동
* 프로덕션 빌드를 대상으로 다음 검사를 추가

```ts
expect(window.__C2_GAME__).toBeUndefined();
```

그리고 프로덕션 URL에 `?e2e=1`을 붙여도 런 시간이 600초에서 24초로 바뀌지 않는지 검사해야 합니다.

---

## 1.2 서브패스 배포 시 에셋이 깨질 가능성

`src/game/assets.ts`의 에셋 경로가 `/assets/...` 형태의 루트 절대경로이고, `vite.config.ts`에 명시적인 `base` 전략이 없습니다.

따라서 다음처럼 도메인 루트에 배포하면 대체로 괜찮습니다.

```text
https://example.com/
```

하지만 GitHub Pages 프로젝트 배포처럼 서브패스를 쓰면 깨질 수 있습니다.

```text
https://user.github.io/relaywake/
```

브라우저가 다음을 요청하기 때문입니다.

```text
https://user.github.io/assets/...
```

실제로 필요한 경로는 다음입니다.

```text
https://user.github.io/relaywake/assets/...
```

### 권장 수정

에셋을 상대경로로 보관하고 한 곳에서 해결하십시오.

```ts
function publicAsset(path: string): string {
  return `${import.meta.env.BASE_URL}${path.replace(/^\/+/, '')}`;
}
```

배포 대상에 따라 Vite `base`도 명시해야 합니다.

```ts
export default defineConfig({
  base: './',
});
```

또는 GitHub Pages 전용으로:

```ts
base: '/relaywake/',
```

프로덕션 미리보기뿐 아니라 **실제 서브패스 서버에서 모든 이미지·오디오의 HTTP 상태가 200인지** 검사하는 배포 스모크 테스트가 필요합니다.

---

## 1.3 적 생성 스크립트가 깨끗한 저장소에서 재현되지 않음

`package.json`에는 `generate:enemies`가 공개 명령으로 존재하지만, `scripts/generate-enemy-sprites.mjs:10`은 저장소 외부의 다음 파일을 찾습니다.

```text
../monster-generator.html
```

해당 파일이 ZIP에 없어서 실제로 명령을 실행하면 `scripts/generate-enemy-sprites.mjs:285-287`에서 실패합니다.

문서에서 언급하는 다음 생성기 파일도 포함되어 있지 않습니다.

* `pixel-character-maker-mongle.html`
* `kite-fire-v2.html`
* `monster-generator.html`

이는 게임 실행 자체를 막지는 않더라도, 공개 소스에서 “자산 생성 파이프라인이 재현 가능하다”는 인상을 주기 때문에 정리해야 합니다.

### 선택지는 두 가지입니다

**재현 가능한 소스 공개를 목표로 한다면**

* 생성기 파일을 `tools/generators/` 아래 포함
* 상대경로를 저장소 내부 기준으로 변경
* 생성기 버전과 시드를 기록
* 생성 결과 해시 검사 추가
* CI에서 `npm run generate:enemies -- --check` 수행

**생성기를 공개할 수 없다면**

* `generate:enemies` 명령 제거
* 해당 PNG를 “고정된 최종 산출물”로 설명
* 재생성 가능하다는 문구 제거
* 원본·라이선스·수정 이력만 명확히 기록

---

## 1.4 CI와 릴리스 게이트 복원

`VALIDATION.md:44-47`에 따르면 CI가 제거된 상태이며, ZIP에도 `.github/workflows`가 없습니다.

현재 `npm run check` 구조는 좋지만, 로컬 명령만 존재하면 최종 공개 직전에 다음 문제가 쉽게 발생합니다.

* 생성 파일 누락
* 프로덕션 빌드에서만 발생하는 오류
* 에셋 경로 오류
* 테스트 브리지 유출
* 문서와 실제 데이터 불일치

최소 릴리스 게이트는 다음 순서를 권장합니다.

```text
npm ci
→ typecheck
→ unit test + coverage
→ generator/asset integrity check
→ production build
→ bundle-size budget
→ desktop E2E
→ mobile E2E
→ WebKit E2E
→ deployed URL smoke test
```

Node 20과 현재 주력 LTS 계열을 함께 검사하고, main 브랜치에는 필수 체크를 통과해야 병합되도록 설정하는 편이 안전합니다.

이번 검토 환경에서는 의존성 미러가 일부 패키지에 404를 반환해 전체 `npm ci`, 빌드, Vitest, Playwright를 완주하지 못했습니다. 따라서 기존 테스트가 실제 깨끗한 환경에서 모두 통과한다고 단정할 수는 없습니다. 반면 `generate:enemies -- --check`의 누락 파일 오류는 의존성 설치 없이도 재현됐습니다.

---

## 1.5 제품 이름과 내부 식별자 통일

현재 최소 네 계열의 이름이 혼재합니다.

* `package.json`: `signalfall`
* `README.md`, `index.html`: Signalfall
* `VALIDATION.md`, 라이선스·자산 문서: Relaywake
* IndexedDB: `c2-nightfall`
* 전역 테스트 브리지: `__C2_GAME__`
* 마이그레이션 문서: C2

공개 전에 정식 이름을 하나로 고정하는 것이 좋습니다. 사용자가 보는 이름만 바꾸는 것이 아니라 다음을 함께 정리해야 합니다.

* 패키지 이름
* HTML title/description
* README 및 문서
* 에셋 저작권 표기
* 저장소 이름
* 브라우저 DB 이름
* 테스트 전역 변수
* 스크린샷 및 릴리스 파일명

기존 베타 사용자의 데이터를 보존해야 한다면 DB를 단순히 새 이름으로 바꾸지 말고, 구 DB를 읽어 새 DB로 옮기는 일회성 마이그레이션을 넣어야 합니다.

---

# 2. 전투 코어에서 가장 중요한 구조 문제

## 2.1 모든 능력이 하나의 범용 `WeaponStats`를 공유함

`src/game/systems/AbilityDirector.ts:70-89`의 `effectiveStats()`는 행동 유형과 무관하게 다음 값을 광범위하게 증폭합니다.

* damage
* cooldown
* projectileSpeed
* count
* radius
* knockback

문제는 각 행동이 같은 필드를 전혀 다르게 해석하거나 아예 사용하지 않는다는 점입니다.

### 현재 확인되는 오작동

| 능력 유형  | 현재 문제                                     |
| ------ | ----------------------------------------- |
| 수류탄    | `count`가 올라가도 `fireGrenade()`는 항상 한 발만 생성 |
| 화염병    | `count` 증가가 실제 투척 수에 반영되지 않음              |
| 바주카    | 범용 발사 수 증가가 실제 행동에 일관되게 적용되지 않음           |
| 궤도형 무기 | `radius`가 충돌 반경이 아니라 플레이어와의 궤도 거리로 사용됨    |
| 궤도형 무기 | `projectileSpeed`가 선형 속도가 아니라 회전 각속도로 사용됨 |
| 회복 능력  | 일반 무기용 쿨다운 감소 영향을 받을 수 있음                 |
| 근접 무기  | 발사 수 증가의 의미가 무기별로 제각각                     |

특히 범위 증가 패시브를 먹었는데 불덩이의 크기가 커지는 대신 **궤도 자체가 플레이어에게서 멀어지는 현상**이 생길 수 있습니다. 이는 플레이어가 기대하는 강화와 반대일 수 있습니다.

### 권장 구조

행동별로 스탯 타입을 분리하십시오.

```ts
type AbilityStats =
  | {
      behavior: 'projectile';
      damage: number;
      cooldown: number;
      projectileCount: number;
      projectileSpeed: number;
      hitRadius: number;
      lifetime: number;
      pierce: number;
    }
  | {
      behavior: 'grenade';
      damage: number;
      cooldown: number;
      grenadeCount: number;
      travelTime: number;
      blastRadius: number;
      fragmentCount: number;
    }
  | {
      behavior: 'orbit';
      damage: number;
      orbiterCount: number;
      orbitRadius: number;
      hitRadius: number;
      angularSpeed: number;
      hitInterval: number;
    }
  | {
      behavior: 'melee';
      damage: number;
      cooldown: number;
      reach: number;
      arc: number;
      activeTime: number;
    }
  | {
      behavior: 'zone';
      damage: number;
      cooldown: number;
      zoneRadius: number;
      duration: number;
      tickInterval: number;
    };
```

그리고 패시브도 적용 대상을 명시적으로 제한해야 합니다.

```ts
projectileCount:
  projectile, spreadProjectile, grenade 중 명시된 무기만

area:
  hitRadius, blastRadius, zoneRadius에만 적용
  orbitRadius에는 적용하지 않음

projectileSpeed:
  선형 투사체에만 적용
  angularSpeed에는 적용하지 않음

cooldown:
  공격형 액티브에만 적용
  회복에는 별도 허용 여부 명시
```

Zod 스키마에서 `superRefine()`을 사용해 다음 오류를 시작 시점에 차단하는 것이 좋습니다.

* 행동이 지원하지 않는 스탯을 업그레이드함
* 레벨과 보너스 배열 길이가 맞지 않음
* 키프레임 순서가 잘못됨
* 시작 능력이 패시브이거나 존재하지 않음
* 음수 쿨다운·반경·수량
* `count`가 있는데 실제 행동은 이를 지원하지 않음

이 부분은 밸런싱이 아니라 **코어 정확성 문제**이므로 우선 수정해야 합니다.

---

## 2.2 폭발 무기의 타깃 범위와 실제 비행거리가 크게 다름

현재 비근접 무기는 약 850픽셀 이내의 가장 가까운 적을 선택합니다. 그러나 실제 투사체 도달거리는 훨씬 짧습니다.

대략적인 기본값 기준:

* 수류탄: `260 × 0.72 ≈ 187px`
* 화염병: `230 × 0.78 ≈ 179px`
* 바주카: `210 × 1.4 ≈ 294px`

즉 500~800픽셀 거리의 적을 정상 타깃으로 선택하지만, 투사체는 적에게 닿기 전에 사라질 수 있습니다. 플레이어 입장에서는 “자동 조준이 계속 헛발을 쏜다”고 느끼게 됩니다.

### 권장 수정

다음 중 하나를 택해야 합니다.

1. 무기별 명시적 `targetRange` 안에서만 타깃 선택
2. 목표 거리로부터 비행시간 계산

```ts
const distance = Phaser.Math.Distance.Between(
  originX,
  originY,
  targetX,
  targetY,
);

const travelTime = Phaser.Math.Clamp(
  distance / projectileSpeed,
  minTravelTime,
  maxTravelTime,
);
```

3. 포물선형 무기라면 고정 비행시간을 사용하되 착탄 지점을 목표 좌표로 보간

수류탄·화염병에는 단순 직선 TTL보다 **고정 시간의 포물선/보간 이동**이 시각적으로도 더 잘 맞습니다.

---

## 2.3 근접 판정이 실제 무기 길이보다 넓고 부정확함

근접 공격은 넓은 범위로 후보를 가져온 뒤 각도만 확인하고, 실제 반경을 다시 확인하지 않는 구조입니다. `effect.radius + 50` 고정 여유가 붙어 있어 도끼·검의 유효 사거리가 데이터보다 크게 부풀 수 있습니다.

라이트세이버처럼 각도 조건을 우회하는 동작은 순간적인 360도 전방위 공격처럼 작동할 가능성도 있습니다.

### 권장 방식

공간 해시는 넓은 후보 탐색만 담당하고, 실제 판정은 반드시 별도로 수행해야 합니다.

```ts
const maxDistance = reach + enemy.radius;
const inReach = distanceSquared <= maxDistance * maxDistance;
const inArc = Math.abs(relativeAngle) <= arc * 0.5;

if (inReach && inArc) {
  applyDamage(enemy);
}
```

검처럼 긴 무기는 원형 판정보다 **선분 대 원 충돌**, 휘두르기 무기는 **부채꼴 판정**이 더 적합합니다.

---

## 2.4 보스 반경을 고려하지 않는 광역 후보 탐색

투사체 후보 탐색에 `projectile.radius + 34`, 장판에는 `radius + 38`과 같은 고정 상수가 사용됩니다. 최종 보스 반경은 이보다 크므로, 화면상 가장자리가 겹쳤는데도 후보 탐색에서 빠지는 구간이 생길 수 있습니다.

다음 중 하나가 필요합니다.

* `MAX_ENEMY_RADIUS`를 중앙 상수로 정의
* 공간 해시가 엔티티의 실제 반경을 고려하도록 변경
* broad phase 후 정확한 원-원 충돌 적용

낮은 프레임레이트에서 빠른 투사체가 적을 건너뛰는 터널링도 있으므로, 고속 투사체에는 이전 위치와 현재 위치 사이의 선분 충돌을 적용하는 편이 안전합니다.

---

## 2.5 오브젝트 풀의 상태가 다음 개체에 누출될 수 있음

`acquireSprite()`에서 재사용 스프라이트의 origin을 기본값으로 되돌리지 않습니다.

검 이펙트는 origin을 `(0.12, 0.5)`로 바꾸지만, 같은 텍스처가 궤도형 도끼 등에 재사용될 경우 이전 origin이 남을 수 있습니다. 그러면 회전 중심이나 충돌 시각이 비정상적으로 보입니다.

풀에서 꺼낼 때 다음을 모두 초기화해야 합니다.

```ts
sprite
  .setOrigin(0.5, 0.5)
  .setScale(1)
  .setRotation(0)
  .setAlpha(1)
  .setTint(0xffffff)
  .setFlip(false, false)
  .setBlendMode(Phaser.BlendModes.NORMAL)
  .setVisible(true)
  .setActive(true);
```

가능하면 투사체·근접 이펙트·궤도체의 풀을 분리하는 것이 더 안전합니다.

---

# 3. 보스전은 공개 전 재설계가 필요함

## 3.1 미니보스가 플레이어를 따라잡을 수 없음

캐릭터 기본 이동속도는 대략 164~204인데, 미니보스 속도는 64입니다. 이동속도 패시브까지 포함하면 플레이어는 약 300 수준까지 올라갈 수 있습니다.

현재 별도 추격 보정·재배치·전장 제한이 없으므로 플레이어가 계속 한 방향으로 움직이면 미니보스는 화면 밖에 남아 전투가 사실상 종료됩니다. 보스가 디스폰되지 않는다면 런타임에는 남아 있으면서 게임플레이에는 참여하지 않는 상태가 됩니다.

### 개선안

* 보스가 화면 밖 일정 거리 이상 뒤처지면 진행 방향 앞쪽으로 재배치
* 플레이어와의 거리에 따라 이동속도 가속
* 돌진·점프·워프 패턴 도입
* 보스 등장 시 한시적으로 작은 전장 형성
* 보스 중심에서 너무 멀어지면 보스와 플레이어를 모두 전장 안으로 유도
* 약 20~40초 동안 전투가 이어지는 목표 TTK 설정

단순히 이동속도만 플레이어보다 높이면 도망칠 수 없는 접촉 피해 덩어리가 되므로, **느린 추적 + 명확한 돌진 예고**가 더 좋습니다.

---

## 3.2 최종 보스가 일반 적의 확대판에 가까움

최종 보스는 HP 1,000, 이동속도 70이며 일반 중력 계열 적과 공격 행동이 크게 다르지 않습니다. 일반 적에 적용되는 런 후반 HP 보정도 최종 보스에는 동일하게 적용되지 않는 구조입니다.

반면 10분 성장 후 플레이어는 다음처럼 극단적인 공격 빈도를 만들 수 있습니다.

* 수리검 최대 조합: 약 0.15초마다 최대 9발
* 단검 최대 조합: 약 0.15초마다 최대 7발
* 궤도체: 최대 약 8개
* 장판·폭발·관통 공격 동시 작동

따라서 HP 1,000의 보스는 조합에 따라 몇 초 만에 녹을 가능성이 큽니다. 반대로 피해가 약한 조합은 단조로운 추격전을 오래 해야 할 수 있습니다.

### 권장 보스 구조

**1단계, 100~65% HP**

* 느린 유도탄
* 3방향 또는 5방향 방사탄
* 바닥 위험 구역 예고

**2단계, 65~30% HP**

* 짧은 워프
* 중력장 생성
* 일반 적 소환
* 안전지대 이동 유도

**3단계, 30~0% HP**

* 공격 간격 단축
* 회전형 탄막
* 명확한 최종 필살기 예고

핵심은 HP만 늘리는 것이 아니라 **플레이어의 이동·회피·화력 집중을 동시에 시험하는 것**입니다. 목표 처치시간은 강한 빌드 약 20~30초, 보통 빌드 약 35~50초 정도로 먼저 가정하고 플레이테스트로 조정하는 편이 적당합니다.

보스 투사체 속도 180은 빠른 캐릭터보다 느리므로, 단순 현재 위치 조준만으로는 계속 뒤로 달리는 플레이어를 맞히기 어렵습니다. 일부 공격은 예측 조준을 사용하십시오.

```ts
predicted =
  player.position +
  player.velocity * predictionSeconds;
```

---

## 3.3 10분 이후 상태가 명확하지 않음

최종 보스가 출현한 뒤에도 일반 적 생성이 마지막 곡선의 속도로 계속되고, 타이머는 `00:00` 상태로 남은 채 보스전이 무기한 이어질 수 있습니다.

다음 상태를 명시적으로 분리하는 것이 좋습니다.

```ts
type RunPhase =
  | 'playing'
  | 'bossIntro'
  | 'finalBoss'
  | 'victory'
  | 'defeat';
```

`finalBoss` 단계에서는 다음 중 하나를 선택하십시오.

* 일반 적 스폰 중지
* 보스 패턴에 포함된 소환만 허용
* 일반 적 밀도를 크게 낮춤
* 타이머를 `BOSS` 또는 `OVERTIME 00:24`로 전환
* 지나치게 오래 걸리면 보스 광폭화

---

# 4. 성장 시스템은 “많이 얻는 재미”는 있지만 빌드 정체성이 약함

## 4.1 액티브·패시브 슬롯 제한이 없음

현재 22개 능력을 모두 획득할 수 있고, 레벨업 선택은 최대가 아닌 항목 중에서 비교적 균등하게 섞입니다. 장기적으로는 대부분의 런이 여러 능력을 조금씩 가진 비슷한 형태로 수렴할 수 있습니다.

서바이버류의 반복 플레이를 살리는 핵심은 “이번 런에는 무엇을 포기하고 무엇을 완성했는가”입니다.

### 권장 구조

* 액티브 5~6칸
* 패시브 5~6칸
* 슬롯이 찬 뒤에는 보유 능력 업그레이드만 등장
* 신규 능력과 기존 업그레이드가 최소 하나씩 섞이도록 선택 구성
* 희귀한 슬롯 확장 또는 교체 이벤트는 별도 보상으로 운영
* 추후 진화 조합은 3~5개만 먼저 추가

예:

```text
화염구 + 범위 증가 → 태양핵
단검 + 투사체 수 → 그림자 폭우
번개 + 쿨다운 감소 → 연쇄 폭풍
화염병 + 지속시간 → 불타는 지대
```

진화를 처음부터 많이 만들 필요는 없습니다. 3개 정도만 있어도 플레이어가 빌드를 의도적으로 완성할 이유가 생깁니다.

---

## 4.2 상자 보상이 플레이어 선택보다 더 큰 비중을 차지할 수 있음

현재 곡선을 단순 적분하고 모든 적 처치·경험치 회수를 가정하면 10분 동안 대략 다음 규모입니다.

* 일반 적 생성 요청: 약 2,134회
* 예상 총 경험치: 약 8,345 + 초기 경험치 보석
* 예상 플레이어 레벨: 약 37
* 플레이어가 직접 고르는 레벨업: 약 36회
* 정기 상자: 약 38개
* 미니보스 상자 포함: 약 39개

즉 상자를 모두 획득하면 **무작위 상자 강화 수가 플레이어 직접 선택 횟수와 비슷하거나 더 많아질 수 있습니다.**

이 경우 빌드는 플레이어가 설계하는 것이 아니라 상자가 결정합니다. 상자가 미보유 능력까지 임의로 추가한다면 더 심해집니다.

### 권장 수정

* 일반 상자 주기를 45~60초 수준부터 재검토
* 상자는 기본적으로 이미 보유한 능력만 강화
* 신규 능력 획득은 레벨업 선택 또는 특별 상자에 한정
* 상자 하나당 무조건 여러 능력을 올리지 않음
* 미니보스 상자는 “선택형 3개 중 하나” 또는 진화 재료로 차별화
* 상자 획득 시 어떤 수치가 올랐는지 명확하게 표시

예:

```text
화염구 Lv.4
피해량 18 → 22
재사용 대기시간 0.62초 → 0.56초
```

현재 레벨업 카드의 일반 설명만으로는 선택 결과를 예측하기 어렵습니다.

---

## 4.3 폭탄이 성능과 밸런스를 동시에 흔들 수 있음

현재 폭탄은 화면의 일반 적을 정상 처치 경로로 하나씩 죽입니다. 최대 적 수가 약 420이라면 한 번의 폭탄으로 다음 작업이 한꺼번에 발생할 수 있습니다.

* 수백 번의 피해·죽음 처리
* 수백 개 경험치 보석 가능성
* 코인·회복·자석·추가 폭탄 드롭 판정
* 수백 개 피해 숫자
* 다수 오디오 tone 생성
* 다수 트윈과 텍스트 생성
* 배열 정리와 공간 해시 갱신

모든 적을 처치한다고 가정할 때 한 런의 예상 폭탄 드롭은 약 3.8개입니다. 한 폭탄이 420마리를 처치하면 그 처치들에서 다시 폭탄이 하나 이상 생성될 확률도 무시하기 어렵습니다.

### 권장 처리

폭탄은 일반 공격과 다른 배치 경로를 사용하십시오.

```ts
killAllRegularEnemies({
  suppressDamageText: true,
  suppressPerEnemySound: true,
  mergeExperienceDrops: true,
  capSpecialDrops: true,
});
```

또는 즉사 대신 다음처럼 설계할 수 있습니다.

* 일반 적: 즉사
* 엘리트: 최대 HP의 50~70% 피해
* 보스: 영향 없음 또는 최대 HP의 5%
* 경험치 보석은 5~10개 대형 보석으로 합침
* 특수 아이템 연쇄 드롭 제한

폭탄의 화면 정리 쾌감은 유지하되, 수백 개의 개체를 만드는 방식은 피해야 합니다.

---

## 4.4 코인과 해금 데이터가 아직 실제 시스템이 아님

현재 코인은 획득·저장·표시는 되지만 소비처가 없습니다. 또한 프로필의 다음 필드가 완성된 루프로 연결되지 않습니다.

* `unlockedCharacters`
* `discoveredAbilities`
* 코인
* 캐릭터 선택 해금 조건

캐릭터는 기본적으로 모두 해금된 것으로 보이고, 발견 능력도 메뉴나 도감에 제대로 사용되지 않습니다.

공개판에서는 두 방향 중 하나가 낫습니다.

### 최소 메타 진행을 넣는 경우

* 캐릭터 해금
* 필드 해금
* 색상·이펙트 같은 비전투 외형 해금
* 시작 능력 변형 해금
* 도감
* 특정 조건 도전 과제

영구 공격력 증가처럼 런 난도를 무너뜨리는 메타 강화는 초기에는 피하는 편이 좋습니다.

### 메타 진행을 넣지 않는 경우

* 코인을 `Score` 또는 `Shards collected`로 변경
* 사용하지 않는 프로필 필드 제거
* 런 결과 점수와 최고기록 중심으로 단순화

불완전한 재화 시스템보다 의도적으로 단순한 아케이드 구조가 더 완성도 있어 보입니다.

---

# 5. 일부 수치가 조합에 따라 지나치게 강해질 수 있음

## 생명 흡수

최대 약 8% 확률로 타격당 3 HP 회복하는 구조라면, 다발 투사체·궤도체·장판처럼 초당 타격 수가 높은 빌드에서 회복량이 급증합니다.

권장 방식:

* 내부 쿨다운 0.3~0.5초
* 초당 최대 발동 횟수
* 무기별 발동 계수
* 피해량의 일정 비율 회복
* 장판·지속 피해는 낮은 계수 적용

## 방어력

현재 평면 감산 후 최소 1 피해 구조라면 기본 방어가 있는 캐릭터와 방어 패시브 조합이 저피해 다수 적을 지나치게 무력화할 수 있습니다.

예:

```ts
finalDamage =
  Math.max(
    1,
    rawDamage * percentageReduction - flatReduction,
  );
```

또는 방어 수치에 점감효과를 적용할 수 있습니다.

## 글로벌 쿨다운과 발사 수

쿨다운 배율이 0.3까지 내려가고 발사 수가 +5까지 증가하면 개체 수가 매우 빠르게 폭증합니다. 무기별 최소 쿨다운과 최대 동시 개체 수를 별도로 두는 것이 안전합니다.

```ts
cooldown = Math.max(stats.cooldown, ability.minCooldown);
projectileCount = Math.min(count, ability.maxProjectileCount);
```

---

# 6. 시간 진행과 프레임 독립성을 정비해야 함

`SurvivorScene.ts:277-280`에서 프레임 델타를 최대 0.05초로 제한하고 초과 시간을 버립니다.

이는 큰 프레임 정지를 막는 장점이 있지만, 저사양 기기에서는 다음 문제가 생깁니다.

* 실제 10분보다 런이 더 길어짐
* 적 스폰 수가 기기마다 달라짐
* 지속 피해 횟수가 달라짐
* 이동거리·충돌 결과가 프레임레이트에 따라 달라짐
* 백그라운드 전환 이후 시간 정책이 불명확함

### 권장 구조

고정 스텝 누산기를 사용하십시오.

```ts
const STEP = 1 / 60;
const MAX_STEPS = 5;

accumulator += Math.min(frameDelta, 0.25);

let steps = 0;
while (accumulator >= STEP && steps < MAX_STEPS) {
  simulate(STEP);
  accumulator -= STEP;
  steps++;
}
```

렌더링은 가변 프레임으로 유지하고 전투·이동·스폰만 고정 스텝으로 처리하면 됩니다.

다음 조건에서 같은 시드의 결과가 허용 범위 안에 들어오는지 테스트하십시오.

* 30fps
* 60fps
* 120fps
* 중간에 500ms 정지
* 탭 백그라운드 후 복귀

백그라운드에서는 명시적으로 자동 일시정지하는 것이 가장 예측 가능합니다.

---

# 7. 성능은 평균 상황보다 최대 부하 경로를 검사해야 함

## 긍정적인 부분

* 오브젝트 풀 존재
* 공간 해시 사용
* 비활성 엔티티 압축
* 적 수 상한
* 스폰 곡선 분리
* 화면 흔들림·피해 숫자 설정 제공

기본 방향은 좋습니다.

## 위험한 부분

### `SurvivorScene.ts`가 약 1,920줄

한 클래스가 다음을 모두 담당합니다.

* 런 상태
* 입력
* 이동
* 적 스폰
* 모든 능력 실행
* 충돌
* 드롭
* 경험치
* 레벨업
* 보스
* 이펙트
* 오디오
* 종료 이벤트

당장 대규모 ECS로 다시 만들 필요는 없습니다. 다음 네 부분만 순차적으로 추출하는 것이 좋습니다.

```text
RunLifecycle / SimulationClock
CombatResolver / AbilityBehaviors
RewardAndProgression
PhaserPresentation / Audio
```

특히 능력 행동을 전략 객체나 작은 함수 모듈로 분리하면 앞서 언급한 스탯 의미 문제도 함께 해결됩니다.

### 매 프레임 할당

* 활성 적 수를 구하기 위한 `filter`
* 공간 해시 문자열 키
* 셀 배열과 결과 배열
* 가장 가까운 적 검색 반복
* 각 무기에서 같은 타깃 재계산

활성 적 수는 카운터로 유지하고, 공간 해시 쿼리는 재사용 가능한 scratch 배열을 사용하십시오. 한 프레임에 여러 무기가 같은 최근접 적을 찾는 대신 한 번 계산해 공유하거나, 무기별로 서로 다른 위협을 배정하는 편이 성능과 전투 가독성 모두 좋아집니다.

### 피해 숫자

타격마다 Phaser Text와 tween을 생성·제거하면 고속 무기에서 큰 병목이 됩니다.

* 텍스트 풀
* 동일 적의 짧은 시간 피해 합산
* 화면 최대 표시 수 제한
* 저사양 모드에서는 기본 비활성화

### 오디오

현재 짧은 효과음마다 Web Audio oscillator와 gain을 새로 생성하는 구조라면, 고속 무기에서 오디오 노드가 폭증할 수 있습니다.

* 동시 음 개수 제한
* 같은 종류 음을 30~60ms 단위로 합산
* 짧은 SFX 버퍼 재생 방식
* 사용자 첫 입력에서 AudioContext unlock/resume
* 마스터/효과음 볼륨 분리

### Phaser 초기 로딩

Phaser가 메뉴 진입 시점부터 동기적으로 번들에 들어간다면 초기 화면 로딩 비용이 커집니다. 시작 버튼을 누를 때 게임 모듈을 동적 import하는 방법을 검토할 만합니다.

`vite.config.ts`의 `chunkSizeWarningLimit: 1800`은 큰 번들 경고를 늦출 뿐 실제 최적화는 아닙니다. gzip 기준 예산을 따로 두는 것이 낫습니다.

---

# 8. 저장 시스템은 기반이 좋지만 복구 경로가 필요함

좋은 점은 다음과 같습니다.

* 프로필과 런 저장의 트랜잭션 처리
* 런 ID를 통한 중복 저장 방지
* 설정 저장 큐
* 실패 시 메모리 롤백 구조

그러나 Dexie 스키마가 사실상 v1뿐이고, 읽을 때 엄격한 `parse()`를 사용합니다. 이전 버전 데이터나 손상된 레코드가 하나라도 있으면 초기화가 실패하고, 다음 실행에도 같은 문제가 반복될 수 있습니다.

### 권장 복구 순서

```ts
const parsed = ProfileSchema.safeParse(raw);

if (parsed.success) {
  return parsed.data;
}

const migrated = tryMigrateLegacyProfile(raw);

if (migrated) {
  await saveProfile(migrated);
  return migrated;
}

await quarantineInvalidProfile(raw);
return createDefaultProfile();
```

추가로 다음이 필요합니다.

* DB 버전별 Dexie migration
* 손상 데이터 백업 또는 격리
* 배열 중복 제거
* 날짜 형식 검증
* 실행 ID 검증
* 저장 실패 재시도
* 저장되지 않은 결과 표시
* 런 기록 보관 상한 또는 기록 UI
* 데이터 내보내기·초기화

`bestTimeSeconds`를 단순 최대값으로 저장하면 승리 런에서는 보스를 늦게 잡을수록 “최고” 기록이 될 수 있습니다. 다음을 분리하십시오.

* 최장 생존시간: 최대값
* 최단 클리어시간: 최소값
* 최고 킬
* 최고 레벨
* 최고 점수

런 기록에는 최소한 다음을 남기는 것이 디버깅과 밸런스 분석에 유용합니다.

```ts
{
  gameVersion,
  balanceVersion,
  seed,
  fieldId,
  characterId,
  abilityLevels,
  duration,
  result,
  bossPhase,
  kills,
  damageByAbility
}
```

---

# 9. 테스트에서 추가해야 할 핵심 시나리오

현재 E2E가 메뉴, 테마, 캐릭터, 레벨업, 일시정지, 패배·승리 저장, 설정 실패, 터치 조작까지 포함한 점은 좋습니다. 하지만 최종 공개를 보증하는 테스트는 아직 빠져 있습니다.

## 반드시 추가할 테스트

| 테스트              | 검증 목적                     |
| ---------------- | ------------------------- |
| 프로덕션 브리지 부재      | `?e2e=1` 유출 방지            |
| 서브패스 배포 스모크      | GitHub Pages·정적 호스팅 에셋 검증 |
| 실제 10분 소크        | 메모리 증가·보스 출현·런 완료         |
| 30/60/120fps 동일성 | 프레임 의존 버그 검출              |
| 최대 적 420 + 폭탄    | 배치 처치·드롭·오디오 부하           |
| 최대 발사 수 조합       | 투사체·장판·궤도체 상한             |
| 모든 업그레이드 효과 검사   | 수치가 실제 행동에 반영되는지          |
| 미니보스 추격 검사       | 계속 이동해도 전투가 성립하는지         |
| 최종 보스 TTK 검사     | 지나친 즉사·장기전 방지             |
| 손상된 IndexedDB    | 복구·초기화·격리                 |
| 이전 DB 버전         | 마이그레이션                    |
| 저장 용량/트랜잭션 실패    | 결과 재시도                    |
| 탭 숨김·복귀          | 시간 및 일시정지 정책              |
| 320×568 화면       | 작은 모바일 UI                 |
| iPhone WebKit    | 실제 모바일 브라우저 호환            |
| 키보드 포커스 트랩       | 접근성                       |

단위 테스트 커버리지도 코어·시스템에만 집중되어 있습니다. 수치 자체보다 중요한 것은 다음 파일군에 최소 기준을 두는 것입니다.

* 능력 행동
* 저장소
* 런 종료
* 보스 단계
* 보상/상자
* 컴포넌트 입력
* 프로덕션 구성

---

# 10. UI와 플레이 피드백 개선

## 능력 랙

22개까지 획득 가능하지만 HUD 영역은 약 340px이며 overflow가 감춰지고, 작은 화면에서는 랙 자체가 사라집니다. 실제 보유 빌드를 파악하기 어렵습니다.

슬롯 제한을 도입한 뒤 다음 구성을 권장합니다.

* 액티브 6개: 아이콘, 레벨, 쿨다운
* 패시브 6개: 작은 아이콘과 레벨
* 모바일: 2행 또는 축소형
* 일시정지 화면: 전체 빌드와 정확한 수치

## 캐릭터 시작 능력 표시

메뉴에서 내부 camelCase ID를 직접 표시하기보다 로컬라이즈된 능력명과 짧은 특성을 보여주는 것이 좋습니다.

```text
Fire Master
시작 능력: 화염구
특성: 화염 피해 +15%, 최대 체력 −10
```

현재 다른 캐릭터가 시작 능력만 다르고 실질적 개성이 약하다면, 각 캐릭터에 하나의 명확한 규칙을 주는 것이 좋습니다.

* 이동 중 공격속도 증가
* 체력이 낮을수록 범위 증가
* 근접 처치 시 방어막
* 폭발 무기 재투척 확률
* 궤도 무기 회전 방향 전환 보너스

## 런 종료 화면

현재 기본 통계 외에 다음을 보여주면 반복 플레이 동기가 커집니다.

* 최종 능력과 레벨
* 가장 많은 피해를 준 능력
* 최종 보스 도달 여부
* 이전 기록 대비 변화
* 시드
* 해금 또는 도감 발견
* 다시 같은 캐릭터로 시작

---

# 11. 문서·라이선스·공개 인상

## 실제 능력 수와 문서 불일치

README는 21개 능력을 언급하지만 실제 카탈로그는 22개입니다. 코어 문서 일부도 현재 코드와 맞지 않는 설명이 있습니다. 예를 들어 `finishRun()` 이벤트 발행과 테스트 브리지 설명이 실제 구현과 달라 보입니다.

최종 공개 직전에는 문서를 코드에서 생성하거나 최소한 검사하는 것이 좋습니다.

```text
ability count
character count
enemy count
asset manifest count
game version
save schema version
```

## “Vampire Survivors Clone” 표현

README와 HTML 설명에 “VampireSurvivorsClone” 또는 “Unity Vampire Survivors Clone” 표현이 남아 있습니다. 개발 이력 설명으로는 가능하지만, 첫 화면·검색 결과·README 첫 문장에 있으면 독립 게임보다 복제품 인상이 강해집니다.

공개 설명은 게임 자체의 차별점을 먼저 말하고, 원작·기반 프로젝트의 라이선스와 출처는 `Credits`와 `THIRD_PARTY_NOTICES`에서 투명하게 밝히는 편이 좋습니다.

예:

> Signalfall은 신호가 붕괴한 외계 지대에서 생존하며 능력 네트워크를 조합하는 10분형 액션 로그라이트입니다.

## 에셋 출처

`THIRD_PARTY_NOTICES.md`에는 일부 에셋의 정확한 출처가 확정되지 않았거나, 상위 번들만으로 개별 이미지의 소유 관계를 입증하기 어렵다는 취지의 문구가 있습니다.

공개·공모전·상업 배포 전에는 다음 중 하나를 권장합니다.

* 정확한 원본 URL·저작자·라이선스 버전·수정 여부 기록
* 출처가 불분명한 에셋 교체
* 직접 제작한 에셋은 생성 원본과 작업 이력 보존
* AI 생성 자산은 사용 도구·약관·생성 시점·후처리 내역 기록

이는 법률 자문은 아니지만, 출처 불확실성을 그대로 둔 채 최종 공개하는 것은 피하는 편이 안전합니다.

---

# 최종 공개까지 권장하는 3단계 수정 순서

## 1단계 — Release Lock

이 단계는 게임 외형을 거의 바꾸지 않고 공개 위험을 제거합니다.

1. 프로덕션 E2E 브리지 완전 제거
2. Vite base와 에셋 경로 수정
3. 생성기 누락 해결 또는 스크립트 제거
4. CI 복원
5. 정식 이름 하나로 통일
6. 라이선스·자산 출처 확정
7. README와 실제 능력 수 동기화
8. 프로덕션 배포 스모크 테스트

## 2단계 — Combat Correctness

게임 규칙이 설명대로 작동하게 만드는 단계입니다.

1. 행동별 스탯 타입 분리
2. 수류탄·화염병·바주카 발사 수 반영
3. 궤도 반경과 충돌 반경 분리
4. 타깃 범위와 투사체 도달거리 일치
5. 근접 정확 판정
6. 보스 반경 포함 충돌
7. 스프라이트 풀 전체 상태 초기화
8. RNG를 스폰·보상·선택·전투·연출 스트림으로 분리
9. 고정 타임스텝 도입

## 3단계 — Game Quality

“작동하는 게임”에서 “다시 하고 싶은 게임”으로 가는 단계입니다.

1. 액티브·패시브 슬롯 제한
2. 상자 빈도와 보상 방식 조정
3. 폭탄 배치 처리
4. 미니보스 추격·재배치
5. 최종 보스 3단계 패턴
6. 코인 소비처 또는 점수화
7. 런 결과 빌드 요약
8. 능력 수치 변화 표시
9. 모바일 빌드 HUD
10. 10분 소크·최대 부하 테스트

---

# 공개 기준 체크리스트

다음 조건을 충족하면 “최종 공개판”이라고 부르기 충분합니다.

* 프로덕션에서 `__C2_GAME__`가 존재하지 않음
* `?e2e=1`을 붙여도 게임 규칙이 변하지 않음
* 루트와 서브패스 배포 모두 에셋 404 없음
* 깨끗한 checkout에서 단일 명령으로 전체 검증 통과
* 생성 파이프라인 또는 고정 자산 정책이 재현 가능함
* 모든 업그레이드가 표시된 수치대로 실제 행동을 변경함
* 30/60/120fps에서 런 결과가 유사함
* 폭탄과 최대 개체 상황에서 프레임·메모리가 회복됨
* 미니보스를 단순 직선 이동만으로 무시할 수 없음
* 최종 보스전이 강한 빌드에서도 몇 초 만에 끝나지 않음
* 상자 강화가 플레이어 선택을 압도하지 않음
* 코인·해금 필드가 실제 기능이거나 제거됨
* 손상·구버전 저장 데이터가 앱 시작을 반복적으로 막지 않음
* 제품명·DB·문서·라이선스 명칭이 일치함
* 모든 외부 자산의 출처와 사용 권한을 설명할 수 있음

**핵심적으로는 `전체 리팩터링`보다 `릴리스 무결성 → 능력 스탯 계약 → 보스와 성장 구조` 순서가 맞습니다.** 이 세 부분을 고치면 현재 좋은 기반을 유지하면서도 공개판의 신뢰성과 게임성이 크게 올라갑니다.
