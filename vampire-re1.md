# 종합 판단

이 프로젝트는 단순히 Unity 게임 화면을 웹으로 감싼 형태가 아니라, **게임 규칙과 데이터를 TypeScript로 다시 작성한 실질적인 웹 재구현**입니다. `Phaser`, `Vue`, `Pinia`, `Zod`, `Dexie`, `Vitest`, `Playwright`가 이름만 들어간 것이 아니라 각각 실제 역할을 맡고 있습니다.

가장 잘된 결정은 **매 프레임 게임 상태를 Vue·Pinia에 넣지 않고 Phaser 내부에 유지한 것**입니다. 반대로 가장 큰 약점은 **실제 게임 로직의 절반 가까이가 1,355줄짜리 `SurvivorScene.ts` 한 파일에 집중되어 있다는 점**과, 에셋을 단순 복사해 사용하는 수준에 머물러 **정식 에셋 파이프라인이 없다는 점**입니다.

현재 상태를 구분하면 다음과 같습니다.

* 로컬 실행·포트폴리오·프로토타입: 상당히 적절
* 단기 게임잼·데모 배포: 적절
* 장기 콘텐츠 확장: 리팩터링 필요
* 상업 배포·지속 업데이트: 빌드 재현성, 에셋 출처, 테스트 범위를 먼저 보강해야 함

---

# 1. 실제 기술 스택

| 영역     | 사용 기술                       | 실제 역할                                 |
| ------ | --------------------------- | ------------------------------------- |
| 빌드     | Vite `^8.1.5`               | Vue·TypeScript 번들링과 정적 배포             |
| UI     | Vue `^3.5.18`               | 메뉴, HUD, 설정, 일시정지, 레벨업, 결과 화면         |
| UI 상태  | Pinia `^3.0.3`              | 화면 상태, 프로필, 설정, HUD 스냅숏               |
| 게임 엔진  | Phaser `4.2.1`              | 캔버스 렌더링, 카메라, 스프라이트, 애니메이션, 입력, Tween |
| 언어     | TypeScript `~5.9.2`         | 게임 전체 구현, 엄격한 타입 검사                   |
| 데이터 검증 | Zod `^4.0.0`                | 캐릭터·적·능력·레벨·저장 데이터 검증                 |
| 저장     | Dexie `^4.0.11`             | IndexedDB 기반 프로필·설정·런 기록              |
| 단위 테스트 | Vitest `^4.0.0` + happy-dom | 수치 계산, 스폰, 풀링, 상태 전환 검사               |
| E2E    | Playwright `^1.55.0`        | 메뉴→게임→레벨업→일시정지→결과 흐름                  |
| 그래픽 방식 | Phaser WebGL/Canvas AUTO    | 별도 물리 엔진 없이 직접 좌표·충돌 계산               |
| 오디오    | Web Audio API               | 오디오 파일 없이 사각파 효과음 합성                  |
| 스타일    | 단일 글로벌 CSS                  | UI 프레임워크 없이 직접 작성                     |

### TypeScript 설정

`tsconfig.app.json`은 상당히 엄격합니다.

* `strict`
* `noUncheckedIndexedAccess`
* `exactOptionalPropertyTypes`
* `noImplicitReturns`
* `noUnusedLocals`
* `noUnusedParameters`
* `noFallthroughCasesInSwitch`
* `verbatimModuleSyntax`

따라서 타입 설정 자체는 좋습니다. 다만 `skipLibCheck: true`이므로 외부 라이브러리 타입 전체를 엄밀히 검사하지는 않습니다.

---

# 2. 전체 실행 파이프라인

```text
index.html
   ↓
main.ts
   ↓
Vue App + Pinia 생성
   ↓
Dexie에서 프로필·설정 읽기
   ↓
메인 메뉴와 캐릭터 선택
   ↓
GameViewport 마운트
   ↓
GameController
   ↓
Phaser.Game 생성
   ├─ BootScene
   │    ├─ 39개 텍스처 로드
   │    ├─ 캐릭터·적 애니메이션 생성
   │    └─ SurvivorScene 시작
   │
   └─ SurvivorScene
        ├─ 플레이어 생성
        ├─ SeededRandom 생성
        ├─ AbilityDirector 생성
        ├─ SpawnDirector 생성
        ├─ 적·투사체·드롭·충돌 처리
        └─ Vue에 HUD·레벨업·결과 이벤트 전송
```

## Vue → Phaser 명령 흐름

Vue는 Phaser Scene을 직접 참조하지 않고 다음 경계를 통합니다.

```text
Vue 컴포넌트
  → GameController
  → sceneBridge
  → 현재 SurvivorScene
```

일시정지, 재개, 능력 선택, 모바일 조이스틱 입력이 이 경로를 사용합니다.

## Phaser → Vue 이벤트 흐름

```text
SurvivorScene
  → gameEvents
  → App.vue
  → Pinia session store
  → Vue HUD·오버레이 갱신
```

전달되는 이벤트는 다음과 같습니다.

* `ready`
* `hud`
* `levelUp`
* `paused`
* `runEnded`
* `toast`

이 양방향 경계는 이번 프로젝트에서 가장 잘 설계된 부분입니다. Phaser의 고빈도 상태가 Vue 반응성 시스템으로 유입되지 않기 때문에 불필요한 DOM 업데이트를 피합니다.

---

# 3. 프레임 단위 게임 파이프라인

`src/game/scenes/SurvivorScene.ts:224–251`의 매 프레임 처리 순서는 다음과 같습니다.

```text
1. 경과 시간 증가
2. 배경 스크롤
3. 플레이어 이동
4. 능력 쿨다운 감소
5. 자동 회복
6. 적 스폰
7. 적 이동·AI·공격
8. 적 공간 해시 재구축
9. 회전 무기
10. 자동 무기 발사
11. 근접 공격
12. 투사체 이동·충돌
13. 화염·중력 지역 처리
14. 아이템 이동·획득
15. 비활성 엔티티 배열 압축
16. HUD 스냅숏 전송
```

주요 런타임 수치는 다음과 같습니다.

* 일반 런: 600초
* 미니보스: 300초
* 최종 보스: 600초
* 일반 적 최대치: 420
* E2E 적 최대치: 90
* 공간 해시 셀 크기: 96px
* HUD 전송 간격: 0.08초, 약 12.5Hz
* 비활성 객체 배열 정리: 0.7초마다
* 프레임 델타 최대치: 0.05초
* 시작 경험치 보석: 25개

## 물리·충돌

Phaser Arcade Physics나 Matter Physics는 사용하지 않습니다. 대신 다음을 직접 구현했습니다.

* 속도 기반 위치 적분
* 원형 충돌 검사
* 적 근접 질의를 위한 공간 해시
* 직접 구현한 넉백
* 적끼리의 간단한 분리
* 투사체 관통 횟수
* 영역 공격과 근접 부채꼴 판정

420개 정도의 적을 목표로 한 간단한 생존 게임에는 합리적인 선택입니다. 물리 엔진 의존성이 없고 밸런스를 직접 통제하기 쉽습니다.

다만 완전히 결정론적인 시뮬레이션은 아닙니다. 시드 RNG는 결정론적이지만 게임은 고정 60Hz 시뮬레이션이 아니라 **가변 delta 기반**입니다. 프레임 속도와 입력 타이밍이 다르면 충돌 결과까지 완전히 동일해지지는 않습니다.

또한 delta를 0.05초로 제한하므로 20FPS 이하로 크게 떨어지거나 탭이 장시간 멈췄다가 돌아오면 게임이 실제 시간만큼 따라잡지 않고 **시뮬레이션 자체가 느려집니다**. 안정성에는 유리하지만 저성능 기기에서는 10분 런이 실제로 10분보다 길어질 수 있습니다.

---

# 4. 아키텍처 평가

## 잘된 부분

### ① 실시간 상태를 Pinia에서 제외

적, 투사체, 아이템, 무기 상태는 Phaser Scene의 일반 배열에 있습니다. Pinia에는 약 12.5Hz HUD 스냅숏만 전달합니다.

이는 Vue와 Phaser를 결합할 때 권장되는 구조입니다. 적 400개의 좌표를 Vue 반응형 객체로 만들었다면 성능과 디버깅 난도가 크게 나빠졌을 것입니다.

### ② 순수 TypeScript 모듈 추출

다음 모듈은 Phaser에 거의 의존하지 않습니다.

* `AbilityDirector`
* `SpawnDirector`
* `SpatialHashGrid`
* `ObjectPool`
* XP 계산
* 키프레임 곡선
* 가중치 선택
* Seeded RNG
* 일시정지 상태 전환

이 부분은 Vitest로 직접 검증하기 쉽습니다.

### ③ 데이터 중심 구조

캐릭터, 적, 능력, 레벨이 각각 데이터 파일로 분리되어 있습니다.

* 캐릭터 4종
* 일반 적 6종
* 미니보스·최종 보스 2종
* 액티브 능력 11종
* 패시브 능력 10종

밸런스 수치를 Scene 내부에 흩어 놓지 않은 점은 좋습니다.

### ④ 일시정지 상태 충돌 수정

레벨업 상태와 일반 일시정지 상태를 분리하고, `resolvePauseScreen()`에서 허용되는 전환만 처리합니다.

따라서 이전에 발생하던 다음 충돌을 구조적으로 막았습니다.

```text
레벨업 발생
→ Phaser 내부 정지
→ 일반 paused 이벤트 발생
→ Vue가 레벨업 화면을 일시정지 화면으로 덮어씀
```

현재는 레벨업이 독립된 오버레이 상태를 소유합니다.

---

## 가장 큰 구조적 문제

`SurvivorScene.ts`는 총 **1,355줄**입니다.

이는 다음 비중입니다.

* 전체 `src` TypeScript·Vue 코드의 약 34%
* `src/game` TypeScript 코드의 약 48%

그리고 한 파일이 다음 책임을 모두 갖고 있습니다.

* 플레이어 이동
* 적 AI
* 적 분리
* 적 공격
* 무기 발사
* 투사체 충돌
* 근접 공격
* 영역 공격
* 드롭
* 경험치
* 레벨업
* 보스
* 런 종료
* HUD 생성
* 오디오
* 테스트 포트
* 스프라이트 풀 관리

README의 “Scene은 렌더링과 프레임 조정만 담당한다”는 설명은 **부분적으로만 맞습니다**. 실제로는 핵심 게임 규칙 상당수가 Scene에 남아 있습니다.

완전한 ECS까지 도입할 필요는 없지만 최소한 다음 정도로 나누는 것이 적절합니다.

```text
SurvivorScene             프레임 순서와 Phaser 객체 연결
PlayerSystem              이동·회복·피해
EnemySystem               스폰 이후 AI·공격·분리
WeaponSystem              무기 쿨다운과 발사
ProjectileSystem          투사체 이동·충돌·폭발
PickupSystem              드롭·자석·획득
CombatResolver            피해·넉백·출혈·흡혈
RunProgression            XP·레벨업·보스·종료
AudioService              Web Audio 효과음
```

---

# 5. Unity 데이터 이식 파이프라인

`MIGRATION.md`에는 Unity 구성 요소와 웹 구현의 대응 관계가 잘 정리되어 있습니다.

```text
Unity ScriptableObject
  → TypeScript 객체
  → Zod.parse()
  → Map 및 Director
  → SurvivorScene에서 사용
```

하지만 이것은 자동화된 변환 파이프라인은 아닙니다.

현재 ZIP에는 다음이 없습니다.

* Unity `.asset` 파일을 읽는 변환기
* JSON 중간 산출물
* 원본 데이터 해시
* 생성 스크립트
* 원본과 변환본의 수치 비교 테스트

따라서 현재 방식은 **수동 포팅 후 Zod로 구조 검증**하는 방식입니다. 원본 Unity 수치가 바뀌면 TypeScript 파일도 사람이 직접 수정해야 합니다.

Zod도 구조적 오류는 잡지만 의미적 오류까지 잡지는 않습니다. 예를 들어 현재 스키마는 다음을 보장하지 않습니다.

* 키프레임이 시간순으로 정렬되어 있는가
* 첫 키프레임이 0이고 마지막이 1인가
* `miniBossTimeSeconds <= durationSeconds`인가
* 모든 스폰 확률 벡터 길이가 동일한가
* 액티브 능력의 보너스 개수가 `maxLevel - 1`과 일치하는가
* 패시브 능력과 behavior 조합이 올바른가

즉, **Zod 사용은 실제적이지만 아직 기본 구조 검증 수준**입니다.

---

# 6. 에셋 구성

총 40개 이미지 파일이 들어 있습니다.

| 분류     |     개수 |        파일 크기 |
| ------ | -----: | -----------: |
| 배경     |      3 |     약 4.05MB |
| 캐릭터    |      4 |      약 3.8KB |
| 적      |      8 |      약 8.7KB |
| 무기     |     13 |      약 4.0KB |
| 아이템    |      7 |      약 6.4KB |
| UI     |      5 |     약 44.8KB |
| **합계** | **40** | **약 4.12MB** |

배경 3개가 전체 이미지 용량의 약 **98.4%**입니다.

## 배경 사용

### 게임 화면

* `dirt.png`: 512×512, 기본 TileSprite
* `dirt-red.png`: 2048×2048, ADD 블렌딩의 붉은 오버레이
* Phaser 카메라와 별개로 화면에 고정하고 플레이어 위치에 따라 타일 좌표만 이동

### 메뉴 화면

* `perlin.jpeg`: 512×512
* CSS 노이즈 오버레이로 직접 사용

게임의 월드는 실제 TileMap이 아니라 다음 방식입니다.

```text
끝없이 반복되는 흙 텍스처
+ 플레이어 위치 기반 스크롤
+ 화면 밖 적 생성·제거
```

따라서 “무한 월드”처럼 보이지만 지형 데이터, 장애물, 바이옴, 월드 청크는 없습니다. 정확히는 **무한 평면형 아레나**입니다.

## 캐릭터와 적

* 대부분 96×24
* 24×24 프레임 4개가 가로로 배치
* 최종 보스는 192×48, 48×48 프레임 4개
* Phaser BootScene에서 루프 애니메이션 생성
* 좌우 이동은 별도 방향 프레임 없이 `flipX`
* 캐릭터는 약 2.7배, 적은 약 2.25~2.4배 확대
* `pixelArt`, `roundPixels`, `antialias: false` 설정 사용

이 부분은 해상도와 프레임 규칙이 일관되어 있고, 픽셀 아트 확대 방식도 적절합니다.

## 무기·아이템 재사용

에셋 수를 줄이기 위해 상당한 재사용이 있습니다.

* 회전 도끼가 실제로는 `sword.png` 사용
* 상자가 `coin-10.png`를 금색으로 Tint하여 사용
* 마법사의 발사체가 일반 `grenade.png` 사용
* 폭탄 드롭이 무기 `bomb.png` 사용
* 수류탄 파편이 `shuriken.png` 사용
* 회복·생명흡수 패시브가 기존 아이템·무기 아이콘 재사용

프로토타입에서는 효율적이지만, 최종 그래픽에서는 의미가 다른 개체들이 같은 이미지를 공유해 시각적 구분이 약해질 수 있습니다.

---

# 7. 실제로 확인된 에셋 문제

## ① `play.png`는 실제 PNG가 아님

파일명:

```text
public/assets/ui/play.png
```

실제 내부 포맷:

```text
WebP lossless
```

즉 확장자와 파일 매직 바이트가 다릅니다. 현대 브라우저에서는 보일 가능성이 높지만, 확장자로 MIME을 결정하는 CDN·이미지 도구·검증기에서는 문제가 될 수 있습니다.

또한 아이콘 자체가 검은 삼각형이라 현재의 어두운 레벨업 카드 배경에서는 잘 보이지 않을 가능성이 큽니다. 이 이미지는 `cooldown`과 `moveSpeed` 패시브 아이콘으로 사용됩니다.

권장 조치는 둘 중 하나입니다.

* 파일을 실제 PNG로 변환
* `play.webp`로 이름을 변경하고 경로도 수정

그리고 아이콘 색상은 밝은 색으로 바꾸는 편이 적절합니다.

## ② `potion.png`가 단일 포션 이미지가 아님

`public/assets/pickups/potion.png`는 216×233 크기이며, 내부에는 서로 떨어져 있는 여러 개의 흰색·분홍색 조각 형태가 들어 있습니다.

현재 코드는 이 전체 이미지를 다음처럼 사용합니다.

* 체력 회복 아이템: 스케일 `0.11`
* 회복 패시브 아이콘
* HUD 능력 아이콘

단일 포션 스프라이트가 아니라 복합 이미지 또는 잘못 가져온 원본 이미지에 가깝습니다. 게임에서는 전체 캔버스가 약 24×26px로 축소되어 여러 조각이 함께 보이므로, 회복 아이템으로 인식하기 어려울 수 있습니다.

이 파일은 우선 교체 대상입니다.

## ③ 불필요한 Phaser 프리로드

다음 텍스처는 BootScene에서 로드되지만 게임 Scene에서 사용되지 않습니다.

* `pickup-gem-dark`
* `pickup-gem-light`
* `ui-circle-outline`
* `ui-pause`

다음 파일은 Vue의 `<img>` URL로만 사용되지만 Phaser TextureManager에도 불필요하게 올립니다.

* `ui-circle`
* `ui-square`
* `ui-play`

이 불필요한 프리로드의 원시 RGBA 메모리 환산치는 약 **3.95MiB**입니다. 전송 용량은 작지만, 500~512px UI 이미지가 WebGL 텍스처로 디코딩되는 비용이 큽니다.

## ④ `dirt-red.png`가 용도에 비해 큼

`dirt-red.png`는 2048×2048이고 원시 RGBA 환산 시 약 **16MiB**입니다. 화면 위에서 반복되는 낮은 알파의 색상 오버레이로만 사용되므로 2048 해상도가 반드시 필요한 구조는 아닙니다.

1024×1024로 줄이기만 해도 원시 텍스처 메모리는 약 16MiB에서 4MiB로 줄어듭니다.

`dirt.png`와 `dirt-red.png`는 모두 알파가 완전히 불투명하므로 RGBA PNG일 필요도 없습니다. RGB WebP 또는 적절한 품질의 JPEG를 사용할 수 있습니다.

## ⑤ 에셋 검증 테스트의 사각지대

`asset-integrity.test.ts`는 경로 존재 여부와 주요 스프라이트시트 크기를 검사합니다. 그러나 다음은 잡지 못합니다.

* `play.png`의 실제 포맷 불일치
* CSS에서 직접 참조하는 `perlin.jpeg`
* `potion.png`의 잘못된 콘텐츠
* 사용되지 않는 에셋
* 원본 라이선스와 출처
* 이미지의 투명 여백과 실제 사용 영역

---

# 8. 현재 에셋 파이프라인의 성격

현재는 엄밀한 의미의 에셋 파이프라인이라기보다 다음 방식입니다.

```text
원본 이미지 복사
→ public/assets에 배치
→ assets.ts에서 절대 URL 지정
→ BootScene에서 대부분 일괄 로드
```

장점은 단순하고 디버깅하기 쉽다는 것입니다. 그러나 다음 기능은 없습니다.

* Vite content hash
* 자동 캐시 무효화
* 에셋 압축
* Texture Atlas 생성
* 사용 여부 검사
* 메뉴용·게임용 로딩 그룹 분리
* 에셋 출처 manifest
* 라이선스 manifest
* 원본 해시
* 프레임 메타데이터 자동 검증
* 서브패스 배포 대응

특히 모든 경로가 `/assets/...`인 절대 경로이므로 도메인 루트 배포에는 맞지만 `/games/c2/`와 같은 서브디렉터리 배포에서는 깨질 수 있습니다.

`public`을 유지한다면 최소한 Vite `base` 설정을 배포 환경에 맞춰야 합니다. 더 안정적인 방식은 에셋을 import하거나 `new URL(..., import.meta.url)` 기반 manifest로 전환하는 것입니다.

---

# 9. 초기 로딩과 번들 파이프라인

Phaser와 두 Scene은 `App.vue`가 정적으로 가져오는 `GameController`를 통해 초기 의존성 그래프에 포함됩니다.

즉 사용자가 아직 메뉴만 보고 있어도 브라우저는 다음 코드를 처음부터 내려받습니다.

* Phaser 엔진
* BootScene
* 1,355줄 SurvivorScene
* 게임 시스템
* 게임 데이터

에셋은 런 시작 시 Phaser가 로드하지만 엔진 JavaScript 자체는 초기 페이지 진입 시 포함됩니다.

`vite.config.ts`의 다음 설정도 이 상황과 연관됩니다.

```ts
chunkSizeWarningLimit: 1800
```

큰 Phaser 번들 경고를 완화한 것이지만, 근본적인 코드 분할은 아닙니다.

권장 구조는 다음과 같습니다.

```text
초기 번들
  Vue 메뉴 + Pinia + Dexie

런 시작 시 동적 import
  Phaser + GameController + Scenes + 게임 데이터
```

이를 적용하면 메뉴 첫 화면의 JavaScript 다운로드와 파싱 비용을 줄일 수 있습니다.

---

# 10. 저장 파이프라인

```text
게임 종료
  → RunSummary 생성
  → App.vue
  → profileStore.recordRun()
  ├─ runs 테이블에 런 저장
  └─ profiles 테이블의 누적 통계 갱신
```

저장되는 내용은 다음과 같습니다.

* 런 ID
* 캐릭터
* 승리 여부
* 시간
* 처치 수
* 레벨
* 코인
* 피해량
* 종료 시각

프로필에는 다음이 있습니다.

* 누적 코인
* 최고 시간
* 최고 처치
* 총 런 수
* 해금 캐릭터
* 발견한 능력

## 저장 관련 실제 틈

### `discoveredAbilities`가 갱신되지 않음

`profileStore.recordRun()`은 기존 `discoveredAbilities`를 Set으로 만든 뒤 그대로 다시 저장합니다.

런 결과에는 보유 능력 목록도 없습니다. 따라서 현재 구조에서 `discoveredAbilities`는 계속 빈 배열로 남습니다.

### `unlockedCharacters`가 실질적으로 사용되지 않음

기본 프로필에서 네 캐릭터가 모두 해금되어 있고, MainMenu도 프로필의 해금 목록을 확인하지 않습니다. 현재는 확장용 스키마만 존재하는 상태입니다.

### 런 저장과 프로필 갱신이 원자적이지 않음

런 기록을 먼저 저장하고 프로필을 별도로 저장합니다. 두 번째 저장이 실패하면 다음 상태가 될 수 있습니다.

```text
runs에는 기록 존재
profiles에는 코인·총 런 수 미반영
```

Dexie transaction으로 묶는 편이 안전합니다.

### 설정 저장 실패 처리 부족

`settingsStore.patch()`는 메모리 상태를 먼저 변경한 다음 IndexedDB에 기록합니다. 저장 오류는 App의 토스트 처리로 연결되지 않습니다.

따라서 설정은 화면상 바뀌었지만 새로고침하면 되돌아가는 상황이 생길 수 있습니다.

---

# 11. 테스트·검증 파이프라인

## 구성

단위 테스트 파일은 11개입니다.

검사 범위:

* 능력 레벨 및 패시브 보정
* 에셋 존재와 스프라이트시트 크기
* 키프레임 보간
* 데이터 로딩과 ID 중복
* 일시정지 단축키
* 오브젝트 풀
* Pinia 화면 상태
* 일시정지 상태 전환
* 공간 해시
* 스폰 디렉터
* 경험치 곡선

Playwright는 하나의 통합 흐름을 두 프로젝트에서 실행하도록 되어 있습니다.

* Desktop Chrome
* Pixel 7 프로필

흐름은 다음과 같습니다.

```text
메뉴
→ 게임 시작
→ XP 강제 지급
→ 레벨업 화면 확인
→ 숫자 1로 능력 선택
→ Esc 일시정지
→ 재개
→ 강제 패배
→ 결과 화면 확인
```

## 한계

### 핵심 게임 로직 대부분이 단위 테스트 밖

Vitest coverage 설정은 다음 경로만 포함합니다.

```text
src/game/core/**
src/game/systems/**
```

그러나 실제 전투·AI·드롭·보스·충돌의 대부분은 `SurvivorScene.ts`에 있습니다. 따라서 coverage 수치가 높게 나오더라도 핵심 게임 코드가 충분히 검증됐다고 보기는 어렵습니다.

### 모바일 프로젝트가 실제 터치를 테스트하지 않음

Pixel 7 프로젝트에서도 키보드와 테스트 브리지를 사용합니다. 모바일 조이스틱의 다음 동작은 E2E 검증되지 않습니다.

* pointerdown
* pointermove
* pointercancel
* lostpointercapture
* 화면 회전
* 백그라운드 복귀

### E2E가 프로덕션 빌드를 검사하지 않음

Playwright의 webServer는 다음을 사용합니다.

```text
npm run dev
```

따라서 Vite 개발 서버에서는 통과하지만 `dist` 배포본에서만 발생하는 문제는 잡지 못할 수 있습니다. E2E는 `npm run build` 후 `vite preview`를 대상으로 실행하는 것이 더 안전합니다.

### `npm run check`에 E2E가 없음

현재 `check`는 다음만 실행합니다.

```text
typecheck
unit test
build
```

Playwright는 별도입니다.

---

# 12. 빌드 재현성 상태

ZIP에는 다음이 없습니다.

* `package-lock.json`
* CI 설정
* 빌드된 `dist`
* 테스트 결과 산출물
* coverage 임계치

`packageManager: npm@10.9.2`는 지정되어 있지만 대부분의 의존성이 `^` 범위이기 때문에, 날짜가 다른 두 환경에서 `npm install` 결과가 달라질 수 있습니다.

특히 게임 엔진 Phaser만 `4.2.1`로 정확히 고정되어 있고 Vue, Pinia, Zod, Dexie, Vite, Vitest, Playwright는 범위 버전입니다. `package-lock.json`을 포함하고 CI에서 `npm ci`를 사용해야 재현 가능한 빌드가 됩니다.

이 환경에서도 `npm install`을 실행해 보았지만 내부 npm 레지스트리가 `@playwright/test`와 `@types/node`에 404를 반환하여 실제 `vue-tsc`, Vitest, Vite 빌드, Playwright 실행까지는 진행하지 못했습니다. 이는 ZIP에 포함된 `VALIDATION.md`의 제한 사항과도 일치합니다.

따라서 현재 검증 상태는 다음과 같습니다.

* 코드 구조 및 로직 정적 분석: 완료
* 이미지 40개 실파일·크기·포맷 검사: 완료
* 실제 의존성 설치: 미완료
* 실제 production build: 미확인
* 실제 unit/E2E 실행: 미확인

코드상 실행 가능성은 높아 보이지만, **클린 환경에서 전체 툴체인을 통과했다는 증거는 아직 ZIP에 없습니다.**

---

# 13. 추가로 확인된 구현상 문제

## 일시정지가 Phaser Scene 전체를 정지시키지는 않음

일시정지는 Phaser의 `scene.pause()`가 아니라 `update()` 초반에서 반환하는 방식입니다.

따라서 게임 수치 시뮬레이션은 멈추지만 다음은 계속 움직일 수 있습니다.

* Phaser 애니메이션
* Tween
* 폭발 원형 애니메이션
* 떠오르는 피해 숫자
* 이미 재생 중인 Web Audio 음

게임 규칙 관점에서는 정지하지만 시각적으로는 완전한 정지가 아닙니다.

## 적 공간 해시 순서

현재 순서는 다음과 같습니다.

```text
적 이동·분리
→ 공간 해시 rebuild
→ 무기·투사체 충돌
```

적끼리 분리할 때는 이전 프레임의 해시를 사용합니다. 첫 프레임에는 비어 있고, 이후에도 한 프레임 전 위치를 기준으로 이웃을 찾습니다.

치명적이지는 않지만 적이 빽빽할 때 겹침이나 미세한 떨림을 만들 수 있습니다.

## 마체테·쌍검 방향

`fireSideSlash()`는 플레이어가 바라보는 방향이 아니라 월드 기준 0 또는 π 방향을 사용합니다.

따라서 플레이어가 위나 아래로 이동 중이어도 마체테와 쌍검은 기본적으로 화면 좌우 방향으로 공격합니다. 의도된 연출이 아니라면 facing angle 기준으로 수정해야 합니다.

## 피해량 통계에 오버킬 포함

적 체력이 1 남았을 때 9,999 피해를 주면 `damageDealt`에 9,999가 더해집니다. 특히 전체 폭탄 아이템 때문에 결과 화면 피해량이 실제 적 체력 총합보다 크게 부풀 수 있습니다.

## 최종 보스 상자

최종 보스를 처치하면 상자를 생성한 직후 곧바로 런을 종료합니다. Scene 업데이트가 멈추므로 그 상자는 획득할 수 없습니다.

---

# 14. 에셋·라이선스 문서 평가

루트 `LICENSE`에는 Matthias Broske의 MIT 라이선스가 있고, `THIRD_PARTY_NOTICES.md`는 원본 소스와 복사된 이미지가 MIT로 제공되었다고 명시합니다.

기본적인 고지는 되어 있지만 공개·상업 배포용 추적 문서로는 부족합니다.

누락된 정보:

* 원본 저장소 URL
* 원본 commit 또는 tag
* 에셋별 원본 경로
* 에셋별 저작자
* 에셋별 라이선스
* 수정 여부
* 원본 파일 해시
* 외부 의존성별 정확한 라이선스 버전

또한 공개 메타데이터에는 아직 다음 표현이 남아 있습니다.

* 패키지명: `c2-vampire-survivors-web`
* README: `VampireSurvivorsClone`
* HTML description: `Unity Vampire Survivors Clone`

게임 제목 자체는 `C2 — Nightfall Protocol`로 독립적이므로, 정식 공개 시에는 패키지명과 SEO 설명에서도 클론 표현을 제거하는 편이 브랜딩상 안전합니다.

---

# 15. 우선순위별 개선안

| 우선순위 | 작업                                          | 이유                  |
| ---- | ------------------------------------------- | ------------------- |
| 1    | `package-lock.json` 생성 및 CI에서 `npm ci`      | 재현 가능한 빌드의 기본 조건    |
| 2    | 클린 환경에서 typecheck→unit→build→preview E2E 실행 | 현재 가장 큰 검증 공백       |
| 3    | `play.png`, `potion.png` 수정                 | 실제 화면 품질과 에셋 호환성 문제 |
| 4    | `dirt-red` 축소·압축, 사용하지 않는 preload 제거        | 전송량과 텍스처 메모리 즉시 절감  |
| 5    | Phaser를 런 시작 시 동적 import                    | 메뉴 초기 로딩 경량화        |
| 6    | `SurvivorScene`에서 전투·적·투사체·드롭 분리            | 장기 유지보수와 테스트 가능성 향상 |
| 7    | 에셋 manifest와 출처·라이선스·프레임 정보 추가              | 공개 배포와 자동 검증 준비     |
| 8    | 런 기록과 프로필 갱신을 Dexie transaction으로 결합        | 저장 불일치 방지           |
| 9    | `discoveredAbilities`와 해금 시스템을 구현하거나 제거     | 현재 죽은 메타 진행 데이터 정리  |
| 10   | 고정 timestep 또는 시뮬레이션 accumulator 도입         | 기기별 난이도·결과 차이 감소    |

# 최종 결론

**스택 선택 자체는 적절합니다.** Vue는 UI, Phaser는 실시간 게임, Pinia는 낮은 빈도의 화면·메타 상태, Dexie는 로컬 저장, Zod는 데이터 경계, Vitest와 Playwright는 검증을 맡아 역할 분담도 합리적입니다.

문제는 스택이 과도해서가 아니라 다음 세 지점입니다.

1. **게임 로직이 다시 `SurvivorScene` 한 파일에 집중됨**
2. **에셋이 최적화·추적·분류되지 않은 직접 복사 방식임**
3. **lockfile과 실제 클린 빌드 검증이 없음**

따라서 현재 프로젝트는 **기능이 잘 갖춰진 웹 게임 프로토타입**이지만, 다음 버전에서는 기능 추가보다 먼저 `SurvivorScene` 분리, 에셋 정리, 재현 가능한 빌드 파이프라인을 적용하는 것이 가장 효과적입니다.
